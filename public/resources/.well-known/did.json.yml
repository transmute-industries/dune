get:
  summary: Get Controller Document
  operationId: getControllerDocument
  tags:
    - SCITT
    - SPICE
  responses:
    "200":
      description: Expected response to a valid request
      content:
        application/json:
          schema:
            $ref: "../../schemas/ControllerDocument.yml"
    default:
      $ref: "../../responses/UnexpectedError.yml"